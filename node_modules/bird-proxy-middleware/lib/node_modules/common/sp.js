// https://www.npmjs.com/package/shell-task
var shellSpawn = require('shell-spawn')
var _ = require('lodash')

var sp = function(command, opts) {
    return shellSpawn(command, _.extend({verbose: true}, opts))
}

module.exports = sp;
