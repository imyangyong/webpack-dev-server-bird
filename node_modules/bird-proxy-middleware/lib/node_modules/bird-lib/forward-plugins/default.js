/**
 * 这是一个默认的转发 plugin, 我们只做简单的
 * 
 * @param serverInfo
 * @param urlOptions
 */
module.exports = function(isRetry, serverInfo, urlOptions) {
    if (serverInfo.cookie) {
        urlOptions.headers.cookie = serverInfo.cookie
    }
}